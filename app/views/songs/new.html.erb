<h2>Create a Song</h2>
  <%= form_tag songs_path, method: "post" do %>

    <% if @song.errors.any? %>
      <div id="error_explanation">
        <h2>There were some errors:</h2>
        <ul>
          <% @song.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <% if @song.errors[:title].empty? %>
      <div class="field">
    <% else %>
      <div class="field_with_errors">
    <% end %>
      <%= label_tag "song_title", "Title" %>
      <%= text_field_tag "title", @song.title %>
    </div>

    <% if @song.errors[:released].empty? %>
      <div class="field">
    <% else %>
      <div class="field_with_errors">
    <% end %>      
      <%= label_tag "released", "Released" %>
      <p>Must be either "True" or "False".</p>
      <%= text_field_tag "released", @song.released %>
    </div>

    <% if @song.errors[:release_year].empty? %>
      <div class="field">
    <% else %>
      <div class="field_with_errors">
    <% end %>
      <%= label_tag "release_year", "Release Year" %>
      <br />
      <%= text_area_tag "release_year", @song.release_year %>
    </div>

    <% if @song.errors[:artist_name].empty? %>
      <div class="field">
    <% else %>
      <div class="field_with_errors">
    <% end %>
      <%= label_tag "artist_name", "Artist Name" %>
      <br />
      <%= text_area_tag "artist_name", @song.artist_name %>
    </div>

    <div class="field">
    <%= label_tag "genre", "Genre" %>
    <br />
    <%= text_area_tag "genre", @song.genre %>
  </div>

    <%= submit_tag "Create Song" %>
  <% end %>